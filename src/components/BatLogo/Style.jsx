import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: '20%',
    resizeMode: 'contain',
    marginTop: 20,
    marginBottom: 30,
  },
});

export default styles;
