import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  alert: {
    backgroundColor: '#fff',
    padding: 10,
    borderRadius: 5,
    position: 'absolute',
    bottom: 40,
  },
});

export default styles;
